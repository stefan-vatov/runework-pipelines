name: CI

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        command:
          - npm run build
          - npm run lint
          - npm run typecheck
          - npm test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: stefan-vatov/runework
          path: runework
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts
        working-directory: runework
      - run: npm run build:all
        working-directory: runework
      - run: npm ci --ignore-scripts
      - run: node ./scripts/install-local-runework.mjs --runework-path ../runework/packages/runework
      - run: ${{ matrix.command }}
